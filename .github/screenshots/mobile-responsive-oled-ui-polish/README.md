# Mobile responsive and OLED UI polish — visual evidence

These optimized images are intended for the pull-request description. Full-resolution originals, all required viewports, and detailed provenance are included in the external screenshot archive.

- `login-*` images render the supplied baseline/final `frontend/src/login.html` source directly in Chromium.
- Shell, navigation, settings, queue, and dialog images are clearly labeled source-derived QA fixtures. They exercise the supplied Sonarr structure, labels, theme tokens, unchanged logo, and shared responsive behavior, but they are not represented as a running backend or compiled React application.
- The logo shown in every image uses the original unmodified asset.
